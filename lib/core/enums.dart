enum NutritionType { gel, caffeineGel, bar, bottle, chews, custom }

enum GelCarbs { g30, g40, g45 }

enum BarCarbs { g20, g25, g30 }

enum SweatRate { low, medium, high }

enum FluidRate { low, medium, high }

enum EventDistance { sprint, olympic, middle, long }

enum CalculatorView { input, timeline }

enum TimelineEventType {
  drink,
  gel,
  caffeineGel,
  bar,
  bottle,
  chews,
  custom,
  note,
}
